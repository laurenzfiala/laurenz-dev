/**
 * Pass this along with the navigation info.
 */
export interface OverlayHints {
  /**
   * Amount of pixels from the top of the screen to offset the overlay by.
   */
  overlayOffset?: number;
}
